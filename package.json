{
  "name": "async",
  "version": "1.0.0",
  "description": "Make an app executing JS asynchronously.  Reflect on it.",
  "main": "hello-async.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "request": "^2.60.0"
  }
}
